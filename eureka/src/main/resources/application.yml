spring:
  application:
    name: eureka-server
    
server:
  port: ${PORT:8761}
  error:
    whitelabel:
      enabled: false


eureka:
  instance:
    hostname: localhost
  client:
    registerWithEureka: false
    fetchRegistry: false
  server:
    enableSelfPreservation: true